apiVersion: v1
kind: ConfigMap
metadata:
  name: internal-mcp-config
  namespace: variant-internal
data:
  HOST: "0.0.0.0"
  PORT: "8080"
  MCP_PATH: "/mcp"
  AZURE_TENANT_ID: "<your-tenant-id>"
  # AZURE_CLIENT_ID is injected by the Workload Identity webhook — do not set here
